# سجلّ القرارات المعمارية

القرارات الكاملة موثّقة كملفات ADR منفصلة في [`docs/adr/`](./docs/adr/). هذا فهرس سريع فقط.

| # | القرار | الحالة |
|---|---|---|
| [0001](./docs/adr/0001-pwa-fullscreen-ios-home-screen.md) | عرض ملء الشاشة على iOS عبر PWA مُضافة للشاشة الرئيسية (بدل سفاري مباشرة) | مُعتمد |
| [0002](./docs/adr/0002-vercel-blob-hobby-storage.md) | اعتماد Vercel Blob (خطة Hobby) للتخزين رغم مخاطر موثّقة | مُعتمد |
| 0003 | لا Supabase — الدخول عبر iron-session + bcrypt، والتخزين على Vercel Blob. راجع خطة البناء في `C:\Users\ibrah\.claude\plans\snazzy-giggling-cake.md`. | مُعتمد |
| 0004 | كل عنصر محتوى = ملف Blob مستقل (كتابة لمرة واحدة) بدل ملف JSON واحد مشترك. انظر التفصيل أدناه. | مُعتمد |
| 0005 | عرض الصور بوضع "احتواء (contain)" بدل "تغطية (cover)". انظر التفصيل أدناه — **انحراف واعٍ عن CONTEXT**. | مُعتمد |
| 0006 | تحسين واجهة الإدارة + إضافة تعديل العناصر واسم اختياري للصور. انظر التفصيل أدناه — **انحراف واعٍ عن CONTEXT** (كان قرارها "واجهة إدارة وظيفية بسيطة"). | مُعتمد |

## 0004 — كل عنصر ملف Blob مستقل بدل ملف JSON مشترك

- **ماذا:** كل عنصر محتوى يُخزَّن كملف Blob منفصل تحت البادئة `items/` (كتابة لمرة واحدة، اسم فريد عبر nonce). التعداد عبر `list()`. التعديل = حذف + إنشاء ملف جديد.
- **لماذا:** التصميم الأول (ملف `content-index.json` واحد يُعاد كتابته overwrite) فشل عمليًا في الإنتاج: فقدان تحديثات (lost update) عند التداخل، وتخزين مؤقت عنيد لرابط عام يُعاد كتابته باستمرار (قراءة تعيد نسخة قديمة) — لم يعالجه أي ضبط للتخزين المؤقت. المشكلة معمارية.
- **البديل المرفوض:** الإبقاء على الملف المشترك مع أقفال/ETag (compare-and-swap) — لا يعالج تخزين الرابط العام المؤقت، وأعقد بلا داعٍ لحجم المشروع.
- **ما قد يكسره مستقبلًا:** التعداد يقرأ كل ملف عنصر (N+1) — مقبول لعشرات العناصر، قد يبطؤ عند المئات.

## 0005 — عرض الصور "احتواء (contain)" بدل "تغطية (cover)"

- **ماذا:** صفحة العرض تعرض الصور بـ `object-contain` (الصورة كاملة، مع شريطين فارغين محتملين عند اختلاف النسبة) بدل `object-cover` المنصوص عليه في CONTEXT.
- **لماذا:** أول محتوى فعلي كان شعار الأكاديمية؛ وضع cover قصّ جزءًا من الاسم فشوّهه. القص يليق بالصور الفوتوغرافية لا بالشعارات والنصوص. إبراهيم اختار contain صراحة بعد رؤية النتيجة.
- **البديل المرفوض:** إبقاء cover ومطالبة الرافع بتجهيز كل صورة بنسبة 16:9 والمحتوى المهم في الوسط — عبء تشغيلي متكرر على كل رفعة.
- **ما قد يكسره مستقبلًا:** الصور الفوتوغرافية غير المطابقة للنسبة يظهر لها شريطان فارغان (ما كان CONTEXT يتجنّبه) — مقبول مقابل ضمان ظهور المحتوى كاملًا. المقاطع أصلًا contain، فالسلوك الآن موحّد.

## 0006 — تحسين واجهة الإدارة وإضافة التعديل

- **ماذا:** (١) زر "تعديل" لكل عنصر يغيّر الاسم والمدة (والرابط لأنواع الروابط) عبر إجراء `update` جديد في مسار API — التعديل داخليًا حذف+إنشاء بنفس الترتيب والـ id (متسق مع [[0004]]). (٢) حقل اسم اختياري للصور والمقاطع (كان الاسم للروابط فقط). (٣) إعادة تصميم بصري كامل للوحة الإدارة (بطاقات، مصغّرات صور، أزرار أيقونية، لوحة ألوان كحلية توافق هوية بارع).
- **لماذا:** طلب إبراهيم صراحةً بعد الإطلاق. الحاجة العملية: تصحيح مدة أو اسم بلا حذف وإعادة إضافة، وتمييز الصور باسم في القائمة.
- **البديل المرفوض:** إبقاء الواجهة "الوظيفية البسيطة" كما نصّ CONTEXT — رُفض بطلب المالك؛ الاستخدام المتكرر كشف حاجة فعلية للتعديل السريع والتمييز البصري.
- **ما قد يكسره مستقبلًا:** لا شيء جوهري — التعديل يعيد استخدام تحقّق الإضافة نفسه (`validateItem`)، ولا يمسّ طبقة التخزين أو الأمان. الرابط غير قابل للتعديل للصور/المقاطع المرفوعة (تغييره = إعادة رفع)، وهو مقصود.
